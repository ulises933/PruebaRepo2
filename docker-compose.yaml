version: "3.9"

services:
  web:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --proxy-headers
    ports:
      - "8000:8000"
    environment:
      - DB_HOST=host
      - DB_PORT=port
      - DB_USER=user
      - DB_PASS=pass

